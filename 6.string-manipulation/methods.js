const username = "farid";
const firstLetter = username.charAt(0).toUpperCase();
const restUsername = username.slice(1);

console.log(firstLetter + restUsername);

// methods masih banyak lagi, silahkan cek dokumentasi di w3school
